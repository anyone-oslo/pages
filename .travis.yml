addons:
  code_climate:
    repo_token: d87d77f1b738c1004ddad858c46206711433daa5c47f644d7495e9afe0012cb0
language: ruby
sudo: false
cache: bundler
services:
  - mysql
rvm:
  - 2.2.3
env:
  - DB=mysql
  #- DB=postgres
script:
  - bundle exec rspec spec
before_script:
  - sh -c "if [ '$DB' = 'mysql' ]; then mysql -e 'create database pages_test;'; fi"
  - sh -c "if [ '$DB' = 'postgresql' ]; then psql -c 'create database pages_test' -U postgres; fi"
  - bundle exec rake db:migrate
